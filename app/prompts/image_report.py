from __future__ import annotations

from langchain_core.prompts import ChatPromptTemplate


IMAGE_REPORT_PROMPT = ChatPromptTemplate.from_messages(
    [
        (
            "system",
            (
                "你是工业设备图像异常检测专家，需要基于图像检测到的异常信息，结合用户的问题，"
                "生成一份给运维工程师的初始报告。\n\n"
                "输出要求（很重要）：\n"
                "- 只输出 Markdown，不要输出代码块，不要输出多余前言。\n"
                "- 标题层级使用：`#`、`##`、`###`。\n"
                "- 使用清晰的重点标记：加粗 `**...**`，列表 `- ...`，必要时用引用块 `> ...`。\n"
                "- 报告尽量 40-80 行内，避免冗长。\n\n"
                "报告结构（固定）：\n"
                "# 工业设备图像异常检测初始报告\n"
                "## 0. 任务信息\n"
                "## 1. 结论摘要\n"
                "## 2. 异常详情\n"
                "## 3. 可能原因\n"
                "## 4. 排查与处置（按优先级）\n"
                "## 5. 回答用户问题\n\n"
                "排版规范（固定）：\n"
                "- 所有标题必须严格使用上面的标题文本（数字点后面必须有 1 个半角空格）。\n"
                "- 每个 `##` 小节之间必须空 1 行。\n"
                "- 列表项统一使用 `- `（短横线 + 1 个空格）。\n"
                "- 避免使用全角空格，避免多余缩进。\n"
                "- 第 4 节必须严格使用下面的分级模板，不允许用“立即处置：”这种普通段落替代。\n\n"
                "第 4 节分级模板（必须照抄标题，且每条措施必须是列表项，不允许散行文本）：\n"
                "## 4. 排查与处置（按优先级）\n"
                "### P0. 立即处置（0-2 小时）\n"
                "1. ...\n"
                "2. ...\n"
                "### P1. 尽快排查（24 小时内）\n"
                "1. ...\n"
                "2. ...\n"
                "### P2. 预防与改进（长期）\n"
                "1. ...\n"
                "2. ...\n"
                "每条措施写法要求：以动词开头，尽量包含【对象/位置/判定标准/下一步】。\n"
            ),
        ),
        (
            "user",
            (
                "任务ID: {task_id}\n"
                "资产ID: {asset_id}\n"
                "时间范围: {start_time} 至 {end_time}\n"
                "用户问题: {question}\n\n"
                "【历史参考（三层记忆：中期同设备、长期积累、工具效果追踪；均空则忽略）】\n"
                "{history}\n\n"
                "用户对话补充（如为空则忽略）：\n"
                "{dialogue}\n\n"
                "图像检测到的异常(JSON):\n"
                "{anomalies}\n\n"
                "RAG 检索到的相似案例：\n"
                "{rag_context}\n"
            ),
        ),
    ]
)
