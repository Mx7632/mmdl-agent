# AGENTS.md

# MMDL-Agent

本项目是一个大学本科毕业设计项目，主题为 **工业异常检测 Agent**。

本文档用于约束 AI 在参与本项目开发时的行为准则、技术规范、目录规范、测试要求与提交规范。AI 在进行任何代码生成、修改、重构、测试或文档编写时，必须严格遵守本文档。

---

## 1. 项目核心上下文

### 1.1 项目目标

本项目专注于工业异常检测，目标是开发并维护一个用于检测工业过程异常的智能化 Agent 系统。

系统应围绕以下能力展开：

* 工业图像异常检测
* 多 Agent 协同分析
* RAG 知识检索与问答
* 异常检测结果解释
* 检测报告生成
* 前后端交互与可视化展示

### 1.2 项目性质

* 本项目为大学本科毕业设计项目。
* 代码实现应优先保证可读性、可维护性和论文展示价值。
* 技术方案应避免过度工程化，但需要保持合理的软件工程规范。
* 涉及架构调整、核心流程变更、接口变更时，应同步整理文档说明。

---

## 2. 环境与基础约束

### 2.1 环境管理

* 必须使用 **Conda** 进行环境管理。
* Conda 环境名称固定为：

```bash
MMDL-Agent
```

* 不应随意引入其他环境管理方式，例如 Poetry、Pipenv、uv 等，除非用户明确要求。

### 2.2 Python 版本

* Python 版本必须固定为：

```bash
Python 3.12.12
```

### 2.3 文件编码

* 所有代码文件、配置文件和文档文件必须使用 **UTF-8** 编码。
* 中文文档和中文注释必须避免乱码。

---

## 3. 技术栈规范

### 3.1 后端框架

* 后端统一使用 **FastAPI**。
* API 设计应保持清晰、稳定、易测试。
* 不应引入 Flask、Django 等其他后端框架。

### 3.2 Agent 框架

* Agent 核心逻辑必须基于以下框架构建：

```text
LangChain
LangGraph
```

* Agent 编排逻辑应保持清晰，避免将所有逻辑堆叠在单个函数或单个文件中。
* 每个 Agent 应具备明确职责。

### 3.3 数据校验

* 所有请求体、响应体、配置对象和内部数据结构应优先使用 **Pydantic V2** 定义。
* 新增 Data Schema 时，应放置在合适的 schema 模块中。
* 避免直接使用无结构的 `dict` 作为长期维护的数据结构。

### 3.4 配置管理

* 配置管理统一使用：

```text
pydantic-settings
```

* 环境变量必须使用 `APP_` 前缀。

示例：

```bash
APP_LOG_LEVEL=INFO
APP_CHROMA_DIR=data/chroma
APP_UPLOAD_DIR=data/uploads
```

* 不应在业务代码中硬编码密钥、路径、模型参数或服务地址。
* 配置读取逻辑应集中管理，避免分散在多个业务文件中。

### 3.5 向量数据库

* 向量数据库统一使用 **ChromaDB**。
* ChromaDB 主要用于 RAG 知识库构建、向量索引存储和检索。
* 向量索引相关文件应存放在 `data/` 目录下的合适子目录中。

---

## 4. 目录结构规范

AI 在创建或修改文件时，必须遵守以下目录划分。

```text
MMDL-Agent/
├── app/
├── web/
├── data/
├── tests/
├── Docs/
└── AGENTS.md
```

### 4.1 `app/`

`app/` 用于存放核心后端逻辑，包括但不限于：

* FastAPI 应用入口
* API 路由
* Agent 编排逻辑
* LangChain / LangGraph 工作流
* RAG 服务
* Prompt 模板
* 配置管理
* 日志与 trace_id 链路追踪
* 工具函数
* 后端核心业务逻辑

建议职责划分：

```text
app/
├── api/
├── core/
├── agents/
├── schemas/
├── services/
├── rag/
├── prompts/
├── tools/
└── utils/
```

### 4.2 `web/`

`web/` 用于存放前端静态资源，包括：

* HTML
* CSS
* JavaScript
* 前端页面资源
* 静态展示逻辑

不得将后端业务逻辑写入 `web/`。

### 4.3 `data/`

`data/` 用于存放运行时数据和本地数据资源，包括：

* ChromaDB 向量索引
* 上传图片
* 运行日志
* 临时检测结果
* 本地缓存数据

注意：

* 不应将大体积运行时数据提交到 Git 仓库。
* 敏感数据、用户上传数据、模型输出缓存等应根据 `.gitignore` 规则排除。

### 4.4 `tests/`

`tests/` 用于存放单元测试和集成测试，包括：

* API 测试
* Agent 流程测试
* RAG 检索测试
* 工具函数测试
* 核心服务测试

新增重要功能时，应优先补充对应测试。

### 4.5 `Docs/`

`Docs/` 用于存放项目文档、技术指南、架构说明和 AI 自动生成的总结文档，包括：

* 架构设计文档
* API 说明文档
* Agent 工作流说明
* RAG 设计说明
* 测试说明
* 重要问答总结
* 论文相关技术材料

所有自动生成的 Markdown 文档应放在 `Docs/` 目录下。

---

## 5. 代码风格与质量要求

### 5.1 基本代码风格

* 代码应清晰、简洁、可维护。
* 优先复用已有模块，避免重复造轮子。
* 函数和类命名应具有明确语义。
* 避免创建过长函数、过大类和职责混乱的模块。
* 不应为了炫技使用复杂写法。
* 不应进行与当前任务无关的大规模重构。

### 5.2 Python 代码规范

* 所有 Python 代码必须包含类型标注。
* 公共函数、核心类和复杂逻辑应包含必要 docstring。
* 异步函数应正确使用 `async` / `await`。
* 不应在异步函数中执行长时间阻塞操作。
* 不应裸用 `except Exception` 后静默吞掉异常。
* 异常信息应通过日志记录，并返回合理的错误响应。

### 5.3 Ruff 规范

* 使用 **Ruff** 进行 lint 和格式化。
* 单行代码长度限制为 **100** 字符。
* 修改 Python 代码后，应尽量运行：

```bash
ruff check .
ruff format .
```

### 5.4 Mypy 规范

* 所有 Python 代码应尽量通过 **Mypy** 类型检查。
* 修改核心逻辑、schemas、services、agents、tools 后，应尽量运行：

```bash
mypy app
```

* 不应通过大量 `Any`、`# type: ignore` 逃避类型问题。
* 如确需使用 `# type: ignore`，必须有明确原因。

---

## 6. 日志与链路追踪规范

### 6.1 trace_id 要求

* 系统必须集成 `trace_id` 链路追踪。
* 每次请求应具备可追踪的 `trace_id`。
* 日志中应包含 `trace_id`，以便定位一次完整请求链路。

### 6.2 异步上下文传递

* 在异步函数、Agent 节点、工具调用、RAG 检索等流程中，应正确传递上下文。
* 并发请求下，不同请求的日志上下文不得互相污染。
* 涉及 LangGraph 节点、executor_node、tools 调用时，应确保 `task_id`、`trace_id` 等上下文信息可追踪。

### 6.3 日志原则

* 日志应服务于调试和问题定位。
* 不应输出敏感信息。
* 不应大量打印无意义日志。
* 错误日志应包含必要上下文，但避免泄露密钥、完整上传内容或用户隐私数据。

---

## 7. Agent 与工具开发规范

### 7.1 Agent 设计原则

* 每个 Agent 应职责单一。
* Agent 间的输入输出结构应清晰。
* 不应将检测、RAG、报告生成、前端响应拼接等逻辑混在同一个 Agent 中。
* Agent 工作流应便于在论文中解释和绘图展示。

### 7.2 LangGraph 工作流

* LangGraph 节点应命名清晰。
* 节点职责应尽量独立。
* 状态对象应使用 Pydantic 或明确类型结构定义。
* 不应在节点之间传递混乱的无结构字典。
* 对核心流程的重大调整，应在 `Docs/` 中说明调整原因和影响。

### 7.3 Tools 编写规范

* 编写 Tools 时，应保持工具调用参数简洁。
* 优先利用 `executor_node` 的参数自动注入机制。
* 例如以下参数应尽量由执行器自动注入，而不是要求上层重复传入：

```text
task_id
image_base64
trace_id
runtime_context
```

* Tool 应专注于单一能力。
* Tool 的输入输出应具备明确类型。
* Tool 不应直接处理前端展示逻辑。

---

## 8. RAG 开发规范

### 8.1 模块职责

RAG 相关逻辑应拆分清晰，至少区分以下职责：

* 文档加载
* 文本切分
* 向量化
* ChromaDB 存储
* 检索
* 上下文组装
* 回答生成

### 8.2 ChromaDB 使用规范

* ChromaDB 数据应存放在 `data/` 目录下的合适位置。
* 不应将向量数据库运行时文件提交到 Git 仓库。
* 知识库路径、集合名称、embedding 配置应通过配置项管理。

### 8.3 RAG 输出规范

* RAG 输出应尽量保留来源信息。
* 回答内容应避免编造不存在的知识来源。
* 如果检索结果不足，应明确返回“未检索到足够信息”或类似提示。

---

## 9. API 开发规范

### 9.1 API 设计原则

* API 路由应放在 `app/api/` 中。
* 请求和响应数据结构应使用 Pydantic V2 Schema。
* API 返回格式应保持一致。
* 不应随意修改已有接口路径、字段名和响应结构。

### 9.2 错误处理

* 参数错误应返回清晰的错误信息。
* 后端异常不应直接暴露完整堆栈给前端。
* 日志中应记录异常上下文和 `trace_id`。
* 前端需要展示的错误信息应简洁明确。

### 9.3 文件上传

* 上传文件必须进行类型、大小和内容校验。
* 不应信任用户上传的文件名。
* 保存文件时应避免路径穿越问题。
* 上传文件路径应由后端统一生成和管理。

---

## 10. 前端开发规范

### 10.1 基本原则

* 前端代码存放在 `web/`。
* 前端应保持简单、清晰，服务于毕业设计系统演示。
* 不应在前端实现后端业务逻辑。
* 不应在前端硬编码敏感配置。

### 10.2 页面功能

前端应围绕以下功能组织：

* 图像上传
* 检测任务提交
* 检测结果展示
* 异常热力图展示
* Agent 执行过程展示
* RAG 问答展示
* Markdown 报告展示

### 10.3 接口调用

* API 地址应集中配置。
* 请求失败时应给出友好提示。
* 不应重复编写相同的请求逻辑。
* 与后端接口变更相关的前端修改，应保持前后端字段一致。

---

可以。你现在的第 **11 节“测试与验证规范”** 还比较笼统，建议单独扩展成下面这一版，直接替换原来的 **第 11 节** 即可。

---

## 11. 测试与验证规范

### 11.1 测试目标

本项目的测试目标是保证工业异常检测 Agent 系统在代码修改、功能扩展和架构调整后仍能稳定运行。

测试应重点覆盖以下内容：

* FastAPI 接口是否能够正常响应。
* Pydantic Schema 是否能够正确校验输入与输出。
* Agent 工作流是否能够按照预期顺序执行。
* LangGraph 节点之间的状态传递是否正确。
* RAG 知识库构建、检索和回答生成是否正常。
* ChromaDB 向量索引是否能够正确读写。
* 图片上传、异常检测、结果返回流程是否完整。
* trace_id、task_id 等上下文信息是否能够正确传递。
* 前端页面是否能够正常调用后端接口并展示结果。

---

### 11.2 测试类型

项目测试应尽量包含以下几类。

#### 11.2.1 单元测试

单元测试用于验证单个函数、类或模块的行为是否正确。

适合编写单元测试的内容包括：

* 工具函数
* 配置读取逻辑
* Pydantic Schema 校验逻辑
* Prompt 构造函数
* RAG 文本切分逻辑
* 文件路径处理逻辑
* 日志 trace_id 生成逻辑
* Agent 节点中的独立处理函数

单元测试应放在：

```text
tests/unit/
```

示例目录：

```text
tests/
└── unit/
    ├── test_config.py
    ├── test_schemas.py
    ├── test_rag_splitter.py
    └── test_trace_context.py
```

---

#### 11.2.2 集成测试

集成测试用于验证多个模块组合后的行为是否正确。

适合编写集成测试的内容包括：

* FastAPI 接口调用流程
* 图片上传接口
* 异常检测接口
* RAG 查询接口
* Agent 工作流执行链路
* ChromaDB 向量库读写流程
* 报告生成流程

集成测试应放在：

```text
tests/integration/
```

示例目录：

```text
tests/
└── integration/
    ├── test_detect_api.py
    ├── test_rag_api.py
    ├── test_agent_workflow.py
    └── test_report_generation.py
```

---

#### 11.2.3 接口测试

接口测试用于验证后端 API 是否符合预期。

重点测试内容包括：

* 请求参数是否合法。
* 响应状态码是否正确。
* 响应 JSON 结构是否稳定。
* 异常输入是否能返回合理错误信息。
* 上传文件类型、大小、路径是否被正确处理。
* 接口是否返回必要的 `trace_id` 或 `task_id`。

推荐使用 FastAPI 的 `TestClient` 或 `httpx.AsyncClient` 编写接口测试。

接口测试应覆盖核心接口，例如：

```text
/v1/detect
/v1/stream
/v1/chat
/v1/generate_report
/v1/rag/build/start
/v1/rag/query
/v1/patchcore/categories
```

---

#### 11.2.4 Agent 工作流测试

Agent 工作流测试用于验证 LangGraph 或 LangChain 编排逻辑是否正确。

重点测试内容包括：

* 工作流是否能够成功启动。
* 每个节点是否按预期执行。
* 节点之间的状态是否正确传递。
* 异常节点是否能够被捕获和记录。
* 工具调用参数是否能够通过 `executor_node` 正确注入。
* `task_id`、`image_base64`、`trace_id` 等上下文参数是否完整传递。
* 最终输出是否包含检测结论、异常解释、RAG 结果或报告内容。

Agent 工作流测试应优先使用小样本、mock 数据或轻量级输入，避免每次测试都依赖真实大模型或大型检测模型。

---

#### 11.2.5 RAG 测试

RAG 测试用于验证知识库构建和检索链路是否正常。

重点测试内容包括：

* 文档是否能被正确加载。
* 文本是否能被正确切分。
* embedding 是否能被正确生成或 mock。
* ChromaDB 是否能正确创建 collection。
* 检索结果是否符合查询语义。
* 当无相关文档时，系统是否能返回合理提示。
* RAG 输出是否包含必要的来源信息。

测试时应优先使用小型测试文档，不应直接依赖大体积生产数据。

推荐测试目录：

```text
tests/rag/
```

---

#### 11.2.6 前端基础验证

如果修改了 `web/` 目录下的前端代码，应至少进行基础验证。

重点验证内容包括：

* 页面是否能正常打开。
* 图片是否能正常上传。
* 请求后端接口是否成功。
* 检测结果是否能正常展示。
* 热力图是否能正常展示。
* Agent 执行时间线是否能正常展示。
* Markdown 报告是否能正常渲染。
* 请求失败时是否有友好提示。

如果项目没有配置前端自动化测试，至少应进行手动验证，并在最终回复中说明验证结果。

---

### 11.3 测试目录规范

推荐测试目录结构如下：

```text
tests/
├── unit/
│   ├── test_config.py
│   ├── test_schemas.py
│   ├── test_utils.py
│   └── test_trace_context.py
├── integration/
│   ├── test_detect_api.py
│   ├── test_rag_api.py
│   ├── test_agent_workflow.py
│   └── test_report_generation.py
├── rag/
│   ├── test_document_loader.py
│   ├── test_text_splitter.py
│   └── test_retriever.py
└── fixtures/
    ├── images/
    ├── docs/
    └── responses/
```

说明：

* `tests/unit/`：单元测试。
* `tests/integration/`：集成测试。
* `tests/rag/`：RAG 相关专项测试。
* `tests/fixtures/`：测试用图片、文档、模拟响应等固定数据。

测试数据应尽量小而稳定，避免放入大体积文件。

---

### 11.4 测试数据规范

测试数据应遵守以下要求：

* 测试图片应放在 `tests/fixtures/images/`。
* 测试文档应放在 `tests/fixtures/docs/`。
* Mock 响应数据应放在 `tests/fixtures/responses/`。
* 不应使用真实隐私数据作为测试数据。
* 不应将大体积模型文件、向量数据库文件或运行缓存提交到测试目录。
* 测试数据应具备稳定性，避免依赖外部网络资源。

---

### 11.5 Mock 与隔离要求

涉及以下内容时，应优先使用 mock 或测试替身：

* 大语言模型调用
* 外部 API 调用
* 大型视觉模型推理
* ChromaDB 真实持久化目录
* 文件系统中的生产数据
* 网络请求
* 时间相关逻辑

测试应尽量做到：

* 可重复运行。
* 不依赖外部网络。
* 不污染真实 `data/` 目录。
* 不修改生产配置。
* 不依赖真实 API Key。

如需测试 ChromaDB，应使用临时目录或测试专用目录。

---

### 11.6 推荐测试命令

修改代码后，应根据修改范围运行对应命令。

#### 11.6.1 运行全部测试

```bash
pytest
```

#### 11.6.2 运行单元测试

```bash
pytest tests/unit
```

#### 11.6.3 运行集成测试

```bash
pytest tests/integration
```

#### 11.6.4 运行 RAG 测试

```bash
pytest tests/rag
```

#### 11.6.5 运行指定测试文件

```bash
pytest tests/integration/test_detect_api.py
```

#### 11.6.6 运行指定测试函数

```bash
pytest tests/integration/test_detect_api.py::test_detect_api_success
```

#### 11.6.7 代码风格检查

```bash
ruff check .
```

#### 11.6.8 代码格式化

```bash
ruff format .
```

#### 11.6.9 类型检查

```bash
mypy app
```

---

### 11.7 不同修改范围对应的测试要求

AI 修改代码后，应根据修改范围选择合适的测试。

| 修改范围              | 至少需要执行的验证                |
| ----------------- | ------------------------ |
| 修改 `app/api/`     | 运行对应 API 测试              |
| 修改 `app/schemas/` | 运行 Schema 单元测试和相关 API 测试 |
| 修改 `app/agents/`  | 运行 Agent 工作流测试           |
| 修改 `app/rag/`     | 运行 RAG 测试                |
| 修改 `app/tools/`   | 运行 Tool 单元测试和相关 Agent 测试 |
| 修改 `app/core/`    | 运行相关单元测试，必要时运行全部测试       |
| 修改 `web/`         | 进行前端页面验证                 |
| 修改配置逻辑            | 运行配置测试和应用启动验证            |
| 修改日志或 trace_id    | 运行 trace_id 相关测试         |
| 修改公共工具函数          | 运行相关单元测试和受影响模块测试         |

---

### 11.8 临时验证脚本规范

如果项目中暂时没有现成测试，AI 可以创建临时验证脚本，但必须遵守以下规则：

* 临时脚本只能用于验证当前功能。
* 临时脚本应放在合适的临时位置，或在任务结束前删除。
* 不应将临时脚本作为正式功能代码提交，除非用户明确要求。
* 如果临时脚本具有长期价值，应整理为正式测试文件放入 `tests/`。
* 最终回复中应说明临时验证方式和结果。

---

### 11.9 测试通过标准

一次修改可以认为基本通过验证，需要满足以下条件：

* 相关测试能够通过。
* Ruff 检查无新增问题。
* Mypy 检查无新增严重类型错误。
* 核心接口能够正常返回。
* Agent 工作流没有明显中断。
* 日志中能够看到正确的 `trace_id`。
* 修改没有破坏既有目录结构和配置方式。

如果部分检查无法运行，必须说明原因。

---

### 11.10 测试失败处理要求

如果测试失败，AI 应：

1. 阅读失败信息。
2. 判断失败是否由本次修改引起。
3. 优先修复与本次任务直接相关的问题。
4. 不应为了让测试通过而删除测试。
5. 不应通过降低测试覆盖、跳过关键断言或隐藏异常来规避问题。
6. 如果失败原因与当前任务无关，应在最终回复中说明。

---

### 11.11 最终回复中的测试说明

每次完成开发任务后，最终回复中必须包含测试说明。

推荐格式：

```text
测试情况：
- 已运行：pytest tests/unit/test_xxx.py
- 已运行：ruff check .
- 未运行：mypy app
- 未运行原因：当前环境未安装 mypy
- 结果：相关测试通过
```

如果没有运行测试，必须明确说明原因，例如：

```text
测试情况：
- 未运行测试。
- 原因：当前任务仅修改文档，未涉及代码逻辑。
```

---

### 11.12 测试相关禁止事项

* 禁止删除已有测试来规避失败。
* 禁止弱化关键断言来掩盖问题。
* 禁止将真实 API Key、Token 或隐私数据写入测试文件。
* 禁止让测试依赖外部网络服务。
* 禁止让测试污染真实 `data/` 目录。
* 禁止提交大体积测试数据、模型权重或 ChromaDB 运行时索引。
* 禁止在未说明原因的情况下跳过与当前修改相关的测试。


---

## 12. 文档管理规范

### 12.1 自动整理

在以下情况结束后，应主动或在用户请求下整理 Markdown 文档：

* 重要 AI 问答
* 复杂功能实现
* 架构调整
* API 变更
* Agent 工作流变更
* RAG 方案变更
* 测试方案变更

### 12.2 文档存放路径

所有自动生成的项目文档应存放于：

```text
Docs/
```

### 12.3 文档命名规范

文档命名应采用以下格式之一：

```text
YYYY-MM-DD-Topic.md
```

或使用具描述性的名称，例如：

```text
api_docs.md
agent_workflow_design.md
rag_design.md
test_plan.md
```

### 12.4 文档内容要求

文档应尽量包含：

* 背景
* 修改内容
* 技术方案
* 涉及文件
* 接口变化
* 测试方式
* 风险说明
* 后续建议

---

## 13. Git 与提交规范

### 13.1 提交时机

每次完成一个相对独立的模块开发、Bug 修复、文档更新或重构后，应及时提交到当前分支。

### 13.2 提交语言

* Git Commit Message 使用中文描述。
* 格式必须遵循：

```text
type(scope): subject
```

### 13.3 type 类型

常用类型包括：

```text
feat: 新功能
fix: 修复问题
docs: 文档更新
style: 代码格式调整
refactor: 代码重构
test: 测试相关
chore: 构建、依赖、配置等杂项
perf: 性能优化
```

### 13.4 scope 范围

常用 scope 包括：

```text
app
api
core
agent
rag
web
docs
tests
config
```

### 13.5 示例

```text
feat(agent): 新增异常检测工作流节点
fix(api): 修复图像上传接口异常处理
docs(rag): 更新知识库构建说明
refactor(core): 优化 trace_id 上下文传递逻辑
test(api): 增加检测接口集成测试
```

### 13.6 PR 描述

发起 Pull Request 时，应简要说明：

* 变更内容
* 实现逻辑
* 涉及模块
* 测试情况
* 对最终用户或系统演示的影响
* 潜在风险

---

## 14. 禁止事项

AI 在参与本项目开发时，必须遵守以下禁止事项。

### 14.1 文档保护

* 未经用户明确允许，禁止修改本 `AGENTS.md` 文件。
* 未经用户明确允许，禁止删除 `Docs/` 中已有的重要文档。
* 不得擅自重写论文相关文档的大段内容，除非用户明确要求。

### 14.2 代码修改限制

* 禁止进行与当前任务无关的大规模重构。
* 禁止随意修改已有 API 路径、字段名和响应结构。
* 禁止随意移动核心目录或改变项目结构。
* 禁止删除已有功能代码，除非用户明确要求。
* 禁止引入与技术栈不一致的新框架。

### 14.3 依赖与环境限制

* 禁止未经说明引入新依赖。
* 禁止改变 Python 版本。
* 禁止改变 Conda 环境名称。
* 禁止引入与 Conda 约束冲突的环境管理方式。

### 14.4 安全限制

* 禁止提交 API Key、Token、密码、私钥等敏感信息。
* 禁止在日志中输出敏感数据。
* 禁止将用户上传文件、运行缓存、向量索引等大体积运行时数据提交到 Git。
* 禁止在接口响应中暴露本机绝对路径。

---

## 15. 任务完成后的回复要求

AI 每次完成开发任务后，应在最终回复中简要说明：

1. 修改了哪些文件。
2. 实现或调整了什么功能。
3. 运行了哪些测试或检查。
4. 是否存在未完成事项或潜在风险。
5. 是否需要用户进一步确认。

回复应简洁明确，避免输出无关内容。

---

## 16. 优先级规则

当不同规则发生冲突时，优先级如下：

1. 用户当前明确指令。
2. 本 `AGENTS.md` 文件。
3. 项目已有代码风格和目录结构。
4. 通用最佳实践。

如果用户当前指令与安全要求、环境限制或项目核心约束冲突，应先向用户说明风险，再等待用户确认。
